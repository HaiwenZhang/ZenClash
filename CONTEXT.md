# ZenClash 领域词汇

ZenClash 协调用户意图、Mihomo 运行事实和操作系统网络状态。本文件只定义项目中的领域语言，不描述具体实现。

## 内核与配置

**托管内核**：
由 ZenClash 启动并拥有生命周期的代理内核；应用退出时，该内核也必须退出。
_避免_：内置内核、后台内核

**外部内核**：
由 ZenClash 之外的进程管理器启动和拥有生命周期、ZenClash 仅连接其控制器的代理内核。
_避免_：托管内核、临时内核

**有效配置**：
选定订阅、用户覆写和运行设置合并后，实际交给代理内核使用的配置。
_避免_：原始订阅、当前配置

## 系统代理

**系统代理意图**：
用户持久选择的系统代理启用状态，不因代理内核暂时离线而自动丢失。
_避免_：系统代理实际状态

**系统代理实际状态**：
从操作系统读取到的当前代理状态，可能因失败、外部修改或代理内核离线而与系统代理意图不同。
_避免_：系统代理意图

**系统代理所有权**：
ZenClash 对自己写入的系统代理状态所持有的可验证凭据，用于判断是否可以安全恢复或释放该状态。
_避免_：系统代理意图、系统代理实际状态

## 代理与流量

**代理选择**：
某个代理组当前实际采用的成员，以代理内核回读结果为准。
_避免_：界面选中项

**逻辑流量采样**：
同一采样时刻产生、由首页、悬浮窗和状态栏共同消费的一份实时流量快照。
_避免_：页面刷新、独立计时器
