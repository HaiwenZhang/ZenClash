# 项目规约

**始终践行 Harness Engineering 方法学，并以行为测试驱动开发。**

本文件是工作入口地图，不是百科全书。稳定知识写入仓库并接受版本控制；实现细节放在对应代码、测试或专项文档中，不在 `AGENTS.md` 重复堆叠。

## 沟通和动手边界

- 默认使用中文；代码、命令、变量名、文件名和协议字段使用英文。
- 结论先行，再给理由；事实性提问直接回答事实。
- 给出真实判断。方案存在风险或有更简单的实现时，应直接指出。
- 用户在提问、讨论方案或征求建议时，只分析和回答，不修改文件。
- 用户明确要求“实现、修复、修改、生成”时，直接执行。
- 跨 crate、架构边界、依赖、发布流程或高风险改动，先给出计划并确认后再实施。
- 新增依赖、改变架构边界或持久化数据结构前，必须先说明决策依据和迁移影响。
- 汇报只保留关键结论、改动路径、验证结果和必要的阻塞信息。

## 项目入口

- [README.md](README.md) ：中文项目介绍、运行方式、功能和数据安全约定。
- [README_en.md](README_en.md) ：英文 README，内容应与中文版保持一致。
- [Cargo.toml](Cargo.toml) ：workspace、Rust 版本、公共依赖和构建配置。
- `crates/zenclash-core`：Mihomo API、内核生命周期、配置、系统代理、流量、日志和持久化。
- `crates/zenclash-ui`：GPUI 页面、组件、应用状态和系统托盘交互。
- `crates/zenclash-i18n/locales/app.yml`：简体中文与英文界面文案。
- `platforms`：平台配置、图标、默认配置和安装包元数据。
- `scripts`：开发、资源生成和跨平台打包脚本。

## 执行纪律

- 先定位任务所属 crate、状态所有者和现有测试，再开始修改。
- 把指令式任务转换为可验证目标；修复 bug 时优先补充复现方式或回归测试。
- 多步骤任务先给简短计划，说明改动点和验证方式。
- 保持差异小、可回滚、可审查，只修改和任务直接相关的文件。
- 不顺手重构、格式化或清理无关代码；由本次修改产生的冗余必须清理。
- 不通过注释报错、放宽 lint 或添加绕过逻辑让代码“先跑起来”，应修复根因。
- 修改完成后主动运行相关验证；无法验证时明确说明原因。
- 不提交订阅地址、控制器密钥、用户配置、日志隐私数据或其他凭据。

## 工程约束

- 使用 Rust edition 2024，并遵循现有 crate 边界和 lint 设置；不在清单中固定 Rust 版本。
- 优先复用已有模块、状态和工具，不为单次使用新增抽象或配置项。
- 公共 API 按 crate 的 `missing_docs` 规则补充文档。
- 阻塞式文件、网络和进程操作不得运行在 GPUI 主线程；使用现有 Tokio runtime 或后台任务。
- UI 优先清晰、稳定、可扫描。复用 gpui-component、语义化主题颜色和统一间距，避免页面私有的硬编码样式。
- 所有用户可见文案写入 `crates/zenclash-i18n/locales/app.yml`，中文和英文必须同步更新。
- Mihomo 是默认正式内核；meow-rs 仅为显式启用的实验后端，不得静默切换或伪装支持 Mihomo 专属能力。
- 不原地改写导入的订阅和 YAML 源文件；生成状态使用现有托管存储和原子写入路径。
- ZenClash 启动的内核进程必须跟随应用生命周期，在正常退出或重启时同步停止。
- 系统代理、TUN、托盘和进程管理属于高风险平台功能，改动时必须覆盖失败恢复和退出路径。

## 简洁和精准

- 用最少代码解决当前问题，不添加需求之外的功能。
- 不为假想需求添加兼容层、可配置项或复杂错误处理。
- 不确定需求含义时明确列出差异；可能改变产品行为时先询问，不默默选择。
- 数据在系统边界处校验，内部逻辑依赖已校验的数据结构。
- 新增第三方依赖前先确认标准库和现有依赖无法满足需求，并检查维护状态与体积成本。

## 验证

按改动范围从最近的单元测试开始，必要时运行完整检查：

```bash
cargo fmt --all -- --check
cargo check --workspace --all-targets --all-features --locked
cargo test --workspace --all-features --locked
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
```

- UI 行为无法自动覆盖时，记录可重复的手工验证步骤、平台和预期结果。
- 真实内核集成测试默认忽略，需要设置 `ZENCLASH_MIHOMO_BINARY` 或 `ZENCLASH_MEOW_BINARY`；验证真实进程行为时不得用 mock 替代。
- 只修改文档时，至少检查链接、路径、命令和双语内容是否一致。
- 只有已实现且已验证的事项才能汇报为完成；未验证的信息必须明确标注。

## 文档和发布维护

- `README.md` 与 `README_en.md` 保持结构和事实一致，允许语言表达自然本地化。
- 产品行为变化应在同一改动中更新对应 README 或专项文档。
- 未经任务明确要求，不修改固定内核版本、校验和、发布目标、签名或安装包流程。
- `AGENTS.md` 只保留导航和高层规则；内容膨胀时应拆分到对应目录的文档中。
