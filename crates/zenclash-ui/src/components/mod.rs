/// Compact floating traffic and outbound-mode window.
pub mod floating;
pub(crate) mod mode;
/// Primary navigation sidebar and outbound-mode values.
pub mod sidebar;
/// Native status icon, menu model, and command routing.
pub mod tray;
